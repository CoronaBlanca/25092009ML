-- DESOFUSCADOR MOONSEC V3 - VERSIÓN DEBUG
-- =========================================

print("🔧 Iniciando diagnóstico...")

-- ⚙️ CONFIGURACIÓN
local WEBHOOK_URL = "TU_WEBHOOK_AQUI"
local GITHUB_RAW_URL = "https://raw.githubusercontent.com/USUARIO/REPO/main/script.lua"

-- TEST 1: Verificar funciones básicas
print("\n📋 TEST 1: Verificando funciones disponibles...")
local functions_available = {
    ["game"] = game ~= nil,
    ["game:HttpGet"] = game and game.HttpGet ~= nil,
    ["loadstring"] = loadstring ~= nil,
    ["request"] = request ~= nil,
    ["writefile"] = writefile ~= nil,
    ["HttpService"] = pcall(function() return game:GetService("HttpService") end)
}

for name, available in pairs(functions_available) do
    print(string.format("  %s %s", available and "✅" or "❌", name))
end

-- Si falta algo crítico, parar aquí
if not game or not loadstring then
    warn("❌ ERROR CRÍTICO: Funciones básicas no disponibles")
    return
end

print("\n✅ Funciones básicas OK")

-- TEST 2: Verificar descarga
print("\n📋 TEST 2: Probando descarga de GitHub...")

local download_success, downloaded_code = pcall(function()
    return game:HttpGet(GITHUB_RAW_URL, true)
end)

if not download_success then
    warn("❌ ERROR descargando:", downloaded_code)
    warn("💡 Posibles causas:")
    warn("   • Link incorrecto")
    warn("   • Repo privado")
    warn("   • Sin conexión")
    return
end

print("✅ Descarga OK:", #downloaded_code, "bytes")

-- TEST 3: Verificar que el código no esté vacío
if #downloaded_code < 100 then
    warn("⚠️ Código muy pequeño, puede estar mal")
    print("Contenido:", downloaded_code:sub(1, 200))
    return
end

print("✅ Tamaño de código válido")

-- Captura simple
local captured_code = nil

-- Hook básico de loadstring
local original_loadstring = loadstring
loadstring = function(code, ...)
    print("🔔 loadstring llamado, tamaño:", #code)
    
    -- Si NO es MoonSec, guardarlo
    if not code:find("MoonSec") and #code > 100 then
        print("✅ Código limpio detectado!")
        captured_code = code
    end
    
    return original_loadstring(code, ...)
end

-- TEST 4: Ejecutar código
print("\n📋 TEST 4: Ejecutando código ofuscado...")
print("⏳ Espera 3 segundos...")

local exec_success, exec_error = pcall(function()
    local func = loadstring(downloaded_code)
    if func then
        func()
    else
        error("loadstring retornó nil")
    end
end)

if not exec_success then
    warn("⚠️ Error al ejecutar:", exec_error)
    print("💡 Esto puede ser normal si el código necesita un entorno específico")
end

-- Esperar captura
wait(3)

-- TEST 5: Verificar captura
print("\n📋 TEST 5: Verificando captura...")

if captured_code then
    print("✅ ¡CÓDIGO CAPTURADO!")
    print("Tamaño:", #captured_code, "bytes")
    print("Primeros 200 caracteres:")
    print(captured_code:sub(1, 200))
    
    -- Guardar
    if writefile then
        local filename = "captured_" .. os.time() .. ".lua"
        writefile(filename, captured_code)
        print("💾 Guardado en:", filename)
    end
    
    -- Intentar enviar a Discord
    if WEBHOOK_URL ~= "TU_WEBHOOK_AQUI" then
        print("\n📤 Intentando enviar a Discord...")
        
        local webhook_success = pcall(function()
            local preview = captured_code:sub(1, 3900)
            local payload = game:GetService("HttpService"):JSONEncode({
                content = "**Código Capturado (" .. #captured_code .. " bytes)**\n```lua\n" .. preview .. "\n```"
            })
            
            request({
                Url = WEBHOOK_URL,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = payload
            })
        end)
        
        if webhook_success then
            print("✅ Enviado a Discord")
        else
            warn("❌ No se pudo enviar a Discord")
        end
    else
        print("⚠️ Webhook no configurado")
    end
    
else
    print("❌ NO SE CAPTURÓ CÓDIGO")
    print("\n💡 Esto significa que MoonSec V3:")
    print("   • No usa loadstring() para código limpio")
    print("   • Ejecuta bytecode directamente en su VM")
    print("   • Necesita métodos más avanzados")
    
    print("\n📝 Alternativas:")
    print("   1. Usar un decompiler de bytecode Lua")
    print("   2. Analizar con IDA Pro + plugins")
    print("   3. Hacer ingeniería inversa manual")
    print("   4. Contactar al autor original")
end

print("\n" .. string.rep("=", 50))
print("🏁 DIAGNÓSTICO COMPLETADO")
print(string.rep("=", 50))

-- Mostrar resumen
print("\nRESUMEN:")
print("• Descarga: " .. (download_success and "✅" or "❌"))
print("• Ejecución: " .. (exec_success and "✅" or "⚠️"))
print("• Captura: " .. (captured_code and "✅" or "❌"))
print("\n💡 Si no capturó código, MoonSec V3 es demasiado")
print("   protegido para desofuscar con este método.")
